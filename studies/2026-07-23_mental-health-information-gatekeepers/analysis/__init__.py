"""Analysis of the search-source audit's citation tables and raw responses.

Modules by function type: `volume` (citations per response, zero-citation
rates), `domains` (diversity and concentration), `composition` (source-type
shares and comparisons), `crosslingual` (language-appropriate routing),
`retrieval` (search invocation from raw responses), `load` (tables, raw
records, shared definitions).

`python3 -m analysis` prints the reported statistics; each is a call into
these modules. Stdlib only.
"""
